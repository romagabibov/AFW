const fs = require("fs");

try {
  // We need to restore it from git? There is no git.
  // We can just manually fix it if we can undo it.
  
  // Actually, I can use git if the directory has a .git
  // Let me just restore from my brain.
} catch(e){}
